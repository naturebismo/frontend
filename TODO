* usar ID's numericos apenas, e não esses base64 incluindo o nome do tipo do Node, o backend não precisa disso
* default image to user avatar
* [React Intl] Error formatting relative time.
RangeError: The date value provided to IntlRelativeFormat#format() is not in valid range.